module.exports =
/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, {
/******/ 				configurable: false,
/******/ 				enumerable: true,
/******/ 				get: getter
/******/ 			});
/******/ 		}
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
Object.defineProperty(__webpack_exports__, "__esModule", { value: true });

// CONCATENATED MODULE: ./node_modules/babel-loader/lib!./node_modules/vue-loader/lib/selector.js?type=script&index=0!./src/slider/src/Slider.vue
//
//
//
//
//
//
//
//
//
//
//
//
//

/* harmony default export */ var Slider = ({
  name: 'slider',
  data: function data() {
    return {
      activeSlide: null,
      left: 0,
      right: 0,
      startPoint: null,
      lastPoint: null,
      moveLeft: 0,
      moveRight: 0,
      length: 200,
      scale: 1,
      currentMax: this.curMax,
      currentMin: this.curMin
    };
  },

  props: {
    minVal: {
      type: Number,
      default: 0
    },
    maxVal: {
      type: Number,
      default: 8100
    },
    curMin: {
      type: Number,
      default: 0
    },
    curMax: {
      type: Number,
      default: 8100
    },
    canFollow: {
      type: Boolean,
      default: true
    },
    lineColor: {
      type: String,
      default: "#b4986e"
    },
    textColor: {
      type: String,
      default: "#b4986e"
    },
    bgColor: {
      type: String,
      default: "#D8D8D8"
    },
    btnNum: {
      type: Number,
      default: 2
    }
  },
  computed: {
    Left: function Left() {
      var val = this.left + this.moveLeft;
      var maxLeft = this.canFollow ? this.Length : this.Length - this.Right;
      return Math.max(0, Math.min(val, maxLeft));
    },
    Right: function Right() {
      var val = this.right - this.moveRight;
      var maxRight = this.canFollow ? this.Length : this.Length - this.Left;
      return Math.max(0, Math.min(val, maxRight));
    },
    CurMaxText: function CurMaxText() {
      return this.currentMax == this.maxVal ? '不限' : this.currentMax + '元';
    },
    Length: function Length() {
      return Math.round(this.length * this.scale);
    },
    PerPriceByLength: function PerPriceByLength() {
      return this.maxVal / this.Length;
    },
    PerLengthByPrice: function PerLengthByPrice() {
      return this.Length / this.maxVal;
    }
  },
  watch: {
    moveLeft: function moveLeft() {
      var val = this.Left * this.PerPriceByLength;
      this.currentMin = Math.min(parseInt(Math.round(val) / 100) * 100, this.currentMax);
    },
    moveRight: function moveRight() {
      var val = (this.Length - this.Right) * this.PerPriceByLength;
      this.currentMax = Math.max(parseInt(Math.round(val) / 100) * 100, this.currentMin);
    }
  },
  methods: {
    getLeftByPrice: function getLeftByPrice(price) {
      return price * this.PerLengthByPrice;
    },
    getRightByPrice: function getRightByPrice(price) {
      return this.Length - price * this.PerLengthByPrice; // (1 - price / this.maxVal) * this.Length
    },
    handleDrag: function handleDrag(e) {
      if (e.target.dataset['slider']) {
        this.activeSlide = e.target;
        this.startPoint = this.lastPoint = e.touches[0].pageX;
      }
      // this.$emit('setScrollStatus', false);
    },
    handleMove: function handleMove(e) {
      var _this = this;

      var curPoint = e.touches[0].pageX;
      if (!this.activeSlide || !this.startPoint || this.timer) {
        return;
      }
      this.timer = setTimeout(function () {
        _this.timer = null;
      }, 8);
      var moveDistance = curPoint - this.startPoint;
      if (this.activeSlide.dataset['slider'] == 'left') {
        if (this.currentMin >= this.currentMax && curPoint > this.lastPoint && this.canFollow) {
          this.moveRight = moveDistance - (this.Length - this.right - this.left);
        }
        this.moveLeft = moveDistance;
      } else {
        if (this.currentMin >= this.currentMax && curPoint < this.lastPoint && this.canFollow) {
          this.moveLeft = moveDistance + (this.Length - this.right - this.left);
        }
        this.moveRight = moveDistance;
      }
      this.lastPoint = curPoint;
    },
    handleEnd: function handleEnd(e) {
      if (this.activeSlide) {
        this.left = this.Left;
        this.right = this.Right;
        this.moveLeft = 0;
        this.moveRight = 0;
        this.activeSlide = null;
        this.startPoint = null;
        this.lastPoint = null;
      }
      this.$emit('getPrice', this.currentMin, this.currentMax);
    },

    /*
     * 设置价格范围
     * this.$invoke('priceSlider', 'setPriceRange', 2000, '不限');
     */
    setPriceRange: function setPriceRange(minPrice, maxPrice) {
      this.currentMin = minPrice;
      this.currentMax = maxPrice == '不限' ? this.maxVal : maxPrice;
      this.left = this.getLeftByPrice.call(this, this.currentMin);
      this.right = this.getRightByPrice.call(this, this.currentMax);
    },

    /*
     * 重置价格
     * this.$invoke('priceSlider', 'resetPrice');
     */
    resetPrice: function resetPrice() {
      this.setPriceRange.call(this, this.minVal, this.maxVal);
    }
  },
  components: {},
  mounted: function mounted() {},
  created: function created() {
    this.scale = document.documentElement.clientWidth / 375;
    this.setPriceRange.call(this, this.currentMin, this.currentMax);
  }
});
// CONCATENATED MODULE: ./node_modules/vue-loader/lib/template-compiler?{"id":"data-v-8af1e710","hasScoped":true,"buble":{"transforms":{}}}!./node_modules/vue-loader/lib/selector.js?type=template&index=0!./src/slider/src/Slider.vue
var render = function () {var _vm=this;var _h=_vm.$createElement;var _c=_vm._self._c||_h;return _c('div',{staticClass:"comp-priceslider",on:{"touchmove":_vm.handleMove,"touchend":_vm.handleEnd}},[_c('div',{staticClass:"comp-text",style:({color:_vm.textColor})},[_c('div',{staticClass:"com-price"},[_vm._v(_vm._s(_vm.currentMin)),(_vm.btnNum==2)?_c('span',[_vm._v("-"+_vm._s(_vm.CurMaxText))]):_vm._e()])]),_vm._v(" "),_c('div',{staticClass:"comp-outer"},[_c('div',{staticClass:"comp-line",style:({background: _vm.lineColor})},[_c('div',{staticClass:"comp-mask comp-mask-left",style:('width: '+_vm.Left+'px;background:'+_vm.bgColor)}),_vm._v(" "),_c('div',{staticClass:"comp-mask comp-mask-right",style:('width: '+_vm.Right+'px;background:'+_vm.bgColor)})]),_vm._v(" "),_c('div',{staticClass:"comp-slider svg left",style:('left: '+_vm.Left+'px'),attrs:{"data-slider":"left"},on:{"touchstart":_vm.handleDrag}}),_vm._v(" "),(_vm.btnNum==2)?_c('div',{staticClass:"comp-slider svg right",style:('right: '+_vm.Right+'px'),attrs:{"data-slider":"right"},on:{"touchstart":_vm.handleDrag}}):_vm._e()])])}
var staticRenderFns = []
var esExports = { render: render, staticRenderFns: staticRenderFns }
/* harmony default export */ var src_Slider = (esExports);
// CONCATENATED MODULE: ./src/slider/src/Slider.vue
function injectStyle (ssrContext) {
  __webpack_require__(1)
}
var normalizeComponent = __webpack_require__(2)
/* script */


/* template */

/* template functional */
var __vue_template_functional__ = false
/* styles */
var __vue_styles__ = injectStyle
/* scopeId */
var __vue_scopeId__ = "data-v-8af1e710"
/* moduleIdentifier (server only) */
var __vue_module_identifier__ = null
var Component = normalizeComponent(
  Slider,
  src_Slider,
  __vue_template_functional__,
  __vue_styles__,
  __vue_scopeId__,
  __vue_module_identifier__
)

/* harmony default export */ var slider_src_Slider = (Component.exports);

// CONCATENATED MODULE: ./src/slider/src/index.js


/* harmony default export */ var src = __webpack_exports__["default"] = ({
    install: function install(Vue) {
        var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

        Vue.component(slider_src_Slider.name, slider_src_Slider);
    }
});

/***/ }),
/* 1 */
/***/ (function(module, exports) {

// removed by extract-text-webpack-plugin

/***/ }),
/* 2 */
/***/ (function(module, exports) {

/* globals __VUE_SSR_CONTEXT__ */

// IMPORTANT: Do NOT use ES2015 features in this file.
// This module is a runtime utility for cleaner component module output and will
// be included in the final webpack user bundle.

module.exports = function normalizeComponent (
  rawScriptExports,
  compiledTemplate,
  functionalTemplate,
  injectStyles,
  scopeId,
  moduleIdentifier /* server only */
) {
  var esModule
  var scriptExports = rawScriptExports = rawScriptExports || {}

  // ES6 modules interop
  var type = typeof rawScriptExports.default
  if (type === 'object' || type === 'function') {
    esModule = rawScriptExports
    scriptExports = rawScriptExports.default
  }

  // Vue.extend constructor export interop
  var options = typeof scriptExports === 'function'
    ? scriptExports.options
    : scriptExports

  // render functions
  if (compiledTemplate) {
    options.render = compiledTemplate.render
    options.staticRenderFns = compiledTemplate.staticRenderFns
    options._compiled = true
  }

  // functional template
  if (functionalTemplate) {
    options.functional = true
  }

  // scopedId
  if (scopeId) {
    options._scopeId = scopeId
  }

  var hook
  if (moduleIdentifier) { // server build
    hook = function (context) {
      // 2.3 injection
      context =
        context || // cached call
        (this.$vnode && this.$vnode.ssrContext) || // stateful
        (this.parent && this.parent.$vnode && this.parent.$vnode.ssrContext) // functional
      // 2.2 with runInNewContext: true
      if (!context && typeof __VUE_SSR_CONTEXT__ !== 'undefined') {
        context = __VUE_SSR_CONTEXT__
      }
      // inject component styles
      if (injectStyles) {
        injectStyles.call(this, context)
      }
      // register component module identifier for async chunk inferrence
      if (context && context._registeredComponents) {
        context._registeredComponents.add(moduleIdentifier)
      }
    }
    // used by ssr in case component is cached and beforeCreate
    // never gets called
    options._ssrRegister = hook
  } else if (injectStyles) {
    hook = injectStyles
  }

  if (hook) {
    var functional = options.functional
    var existing = functional
      ? options.render
      : options.beforeCreate

    if (!functional) {
      // inject component registration as beforeCreate hook
      options.beforeCreate = existing
        ? [].concat(existing, hook)
        : [hook]
    } else {
      // for template-only hot-reload because in that case the render fn doesn't
      // go through the normalizer
      options._injectStyles = hook
      // register for functioal component in vue file
      options.render = function renderWithStyleInjection (h, context) {
        hook.call(context)
        return existing(h, context)
      }
    }
  }

  return {
    esModule: esModule,
    exports: scriptExports,
    options: options
  }
}


/***/ })
/******/ ]);